module.exports = {
    //TODO all the queries used throughout the application will be stationed here.
    getAllUsers: 'SELECT * FROM users',
    getAUser: 'SELECT * FROM USERS WHERE username = "doctor"'
}